"""Core orchestration / framework skills.

These are the skills that compose or support the top-level scan flow —
they don't encode a specific regulatory domain, they're the machinery
that runs domain-specific skills. Scan cycle, fetch, dedup, analyze,
context build, briefing, alert, feedback, memory store/recall.
"""
