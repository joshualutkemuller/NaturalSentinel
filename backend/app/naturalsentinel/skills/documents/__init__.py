"""Document intelligence skills.

Skills that operate on user-uploaded documents via the documents/
package: ingest a document (pipeline → OV/Qdrant/PG), recall context
for a query (tiered retrieval), and follow a process definition
(step-by-step execution against indexed documents).
"""
